model Permission {
    id          Int    @id @default(autoincrement())
    name        String @unique
    descripcion String

    roles Role[]
    users User[]
}

model Role {
    id          Int    @id @default(autoincrement())
    name        String @unique
    descripcion String

    permissions Permission[]
    users       User[]
}
